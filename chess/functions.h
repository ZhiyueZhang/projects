#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <list>
#include <cstdlib>
#include <string>
#include "position.h"

using namespace std;


//bool searchPosVector(vector<Position>, Position); // checks if a given position is an element of given vector of positions

//bool searchPosVector(list<Position>, Position); // checks if a given position is an element of given list of positions

//int chartoint(char); // converts a character to an integer arithmetically

#endif